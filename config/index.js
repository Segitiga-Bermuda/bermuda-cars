const {
    DATABASE_HOST,
    DATABASE_USER,
    DATABASE_PASSWORD,
    DATABASE_NAME,
    JWT_SECRET_KEY,
    PORT
} = require('./environment')

module.exports = {
    DATABASE_HOST,
    DATABASE_USER,
    DATABASE_PASSWORD,
    DATABASE_NAME,
    JWT_SECRET_KEY,
    PORT
}